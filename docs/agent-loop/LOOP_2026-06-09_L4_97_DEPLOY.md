# Agent Loop — Iteracje 3–5 — L4.95/L4.96/L4.97 + DEPLOY NA PRODUKCJĘ

Date: 2026-06-09 (wieczór) · Owner: Claude (loop owner) · Status: **DEPLOYED na D:\ai-council**

## Rój (4 role)

- RESEARCH: web research wzorców Poke/food-ordering (deep-link handoff, checkout u użytkownika,
  anty-wzorzec "udawanie wykonania"). Grok API niedostępny z sesji — research przez WebSearch.
- PLAN/AUDYT: zaprojektował ORDER_DRAFT marker flow (zamiast nowego routingu) + potwierdził reuse
  create_action/approve_response.
- KOD: implementacja L4.95/L4.96 + merge.
- DEBUG/FINAL AUDIT: audyt diffu (PASS; 1 średnie znalezisko naprawione: strip wszystkich markerów;
  + fallback pustej odpowiedzi relay).

## Co weszło

### L4.95 — rollback-safe hands
`workspace_action_snapshot` przed KAŻDYM approved write/append/patch (no rollback => no write),
`workspace_action_undo` + komenda `/undo <plik>`, hint po `/approve`.

### L4.96 — order flow (pizza itd.)
Kontrakt głosu: sekcja ZAMÓWIENIA (zakaz udawania złożenia/opłacenia; slot po slocie; marker
`ORDER_DRAFT: {json}` na końcu odpowiedzi). `extract_order_draft` (strip wszystkich markerów,
walidacja service+items) → `create_order_handoff_action` (typ `order_handoff`, risk R1, deep_link
generowany serwerowo — LLM nie wstrzyknie URL) → stopka `/approve act-...` → `approve_response`
zwraca podsumowanie + link, `external_write_performed=false`. Zero płatności.

### L4.97 — merge z produkcją (KRYTYCZNE)
Windows był do przodu o **Conversation Brain** (18 funkcji: clarify-before-act, food_local
slot-filling, coding flow, brain_decide/loop, sanitize_brain_reply, GPS sloty, BRAIN_SYSTEM_PROMPT,
llm_route wycofany z live path). Procedura: commit L4.93–96 → import stanu Windows jako baza →
`git apply --3way` patcha → 4 konflikty rozwiązane ręcznie (brain prompt + operator fns;
respond-b64: obie strony dodały pamięć wątku niezależnie — wzięta moja wersja ze scrub+facts;
2 konflikty testów). Naprawiona higiena testów (CLARIFICATIONS_FILE pollution).

## Weryfikacja

- Mac: **493/493**. Windows produkcja: **493 passed, 113 subtests** (pytest).
- Smoke respond-b64 na produkcji: "hej"/"chce jedzenie"/"anuluj"/"dziala nowa wersja?" —
  odpowiedzi ludzkie, zero `route=`/`audit_log=`/`[Council]`-debug; brain clarify działa;
  wiszący food-flow domknięty przez "anuluj".
- Listener Telegram: stary proces (11:47) ubity, task wystartowany ponownie — nowe procesy 22:52,
  Status Running. iMessage path nie wymaga restartu (świeży proces per wiadomość).
- Mac bridge launchd: running, wysyła (log: inbound replied + sent imsg-*).
- Backup produkcji: `D:\ai-council\backups\pre-L4.97\` (rollback: skopiować z powrotem + restart taska).
- Bartek powiadomiony przez iMessage (sent-log dedup, bez echa bridge'a).

## Score: 5/5 (pierwszy pełny cykl build→merge→deploy→smoke→notify bez udziału Bartka)

## Ryzyka / obserwacja

- `last_result=267009` scheduled taska — wcześniejszy kod wyjścia; obserwować /front i errors_24h.
- Brain (food_local) i ORDER_DRAFT (Claude) współistnieją: brain łapie wyraźne intencje jedzeniowe,
  ORDER_DRAFT obsługuje zamówienia wychodzące z luźnej rozmowy z Claude — do obserwacji, czy się nie dublują.
- Latencja Claude CLI w iMessage do zmierzenia w realnym użyciu (`AI_COUNCIL_POKE_CHAT_CLAUDE_TIMEOUT`).

## NEXT 3

1. 24h obserwacja: /front, errors, front_quality (debug_metadata=0), latencja odpowiedzi.
2. Proaktywność: `AI_COUNCIL_IMESSAGE_PROACTIVE=true` (morning brief L4.72 + nudges na iMessage) — flaga w .env (decyzja Bartka).
3. GitHub push commitów 88aa965/4c3e73e/0290de7 (+ doki) — za zgodą Bartka.
