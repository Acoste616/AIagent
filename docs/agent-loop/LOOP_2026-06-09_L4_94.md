# Agent Loop — Iteration 1 — L4.94: iMessage Living Front

Date: 2026-06-09 · Owner: Claude (loop owner) · Status: Mac worktree, NOT deployed.

## OBSERVE

- L4.93 (Claude front operator + debug-tail removal) done, 452/452 tests.
- **Finding 1 (critical):** `respond-b64` (iMessage relay) did NOT persist conversation turns —
  thread memory worked only in the Telegram listener. Claude front w iMessage był bez pamięci wątku.
- **Finding 2:** `poke_chat_should_use_llm` gate was tuned for paid Grok API: short natural
  messages ("chce jedzenie") fell to canned local fallback → backend-smell, no Poke feel.
- **Finding 3:** voice contract had no explicit follow-up-question rule.
- Blockers środowiska: brak SSH do Windows i brak sekretów (Grok API) z tej sesji — research
  zewnętrzny przez Groka odłożony; Windows/iMessage smoke wymaga Bartka.

## SCORE → wybrany sprint

Thread memory + natural short-chat + dopytywanie = bezpośrednio Poke feel, iMessage UX,
pamięć; zero nowych ścieżek zapisu zewnętrznego; koszt ~0 (Claude subscription).

## IMPLEMENT

1. `respond_b64_reply(msg)` — nowy pipeline iMessage: persystuje turę user + assistant
   (jak listener Telegrama), zwraca odpowiedź po `strip_debug_metadata`.
2. Gate L4.94: gdy Claude jest operatorem frontu, KAŻDA naturalna wiadomość idzie do LLM
   (poza ACK/status/lokalnym zestawem). Kill-switch: `AI_COUNCIL_POKE_CHAT_CLAUDE_ALL_CHAT=false`.
   Konfiguracja grok-only zachowuje stary konserwatywny gate (testy przypięte).
3. Kontrakt głosu: reguła DOPYTYWANIE — jedno najważniejsze pytanie + propozycja defaultu.
4. Hermes memory parity: `respond_b64_reply` woła też `maybe_auto_extract_facts` (gated,
   quarantine, never-raise) — auto-zapamiętywanie faktów działa teraz również z iMessage,
   nie tylko z Telegrama.

## Routing verified (offline, llm_route off)

- "chce jedzenie" → `/chat` → gate LLM=True → Claude (kontrakt: jedno pytanie o lokalizację/budżet)
- "zrob research o Poke" → `@research` → Grok
- "hej" → `/chat` → local ACK (tanio)
- "co dalej" → `/agent` → local snapshot

## VERIFY

- Mac: py_compile OK; **455/455** testów OK (3 nowe: gate, kontrakt, thread memory + scrub).
- Windows tests / respond smoke / iMessage live smoke: **PENDING (gate: Bartek/SSH)**.

## Score: 4/5

Realny brak (pamięć wątku iMessage) usunięty + krótkie wiadomości przestają brzmieć jak backend.
Punkt odjęty za brak live smoke (środowisko).

## Ryzyka

- `respond-b64` retry może zdublować turę (brak update_id; Telegram ma idempotencję) — niskie,
  kursor bridge'a dedupuje inbound; obserwować.
- Latencja Claude CLI (cold start) przy każdej krótkiej wiadomości — jeśli za wolno w iMessage,
  ustawić `AI_COUNCIL_POKE_CHAT_CLAUDE_ALL_CHAT=false` albo model lżejszy przez
  `AI_COUNCIL_POKE_CHAT_CLAUDE_MODEL`.
- Windows prawdopodobnie przed Makiem — diff przed sync (bez ślepego nadpisania).

## NEXT 3

1. **Hermes memory auto-extract:** po każdej rozmowie wyciągaj fakty/preferencje do pamięci
   (teraz pamięć jest tylko czytana w prompt; zapis ręczny/seed).
2. **Proaktywność:** morning brief + stuck-task nudge przez istniejący iMessage outbox (gated).
3. **Deploy L4.93+L4.94:** diff Mac↔Windows, testy Windows, live smoke "chce jedzenie"
   (oczekiwane: jedno pytanie o lokalizację/budżet, zero route=).
